<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.saamisulaiman.companyofplanes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".boeing" />
        <activity android:name=".airbus" />
        <activity android:name=".sukhoi" />
        <activity android:name=".embraer" />
        <activity android:name=".bombardier" />
        <activity android:name=".fokker" />
        <activity android:name=".atr"></activity>
    </application>

</manifest>